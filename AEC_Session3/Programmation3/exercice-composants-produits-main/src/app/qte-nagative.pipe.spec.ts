import { QteNagativePipe } from './qte-nagative.pipe';

describe('QteNagativePipe', () => {
  it('create an instance', () => {
    const pipe = new QteNagativePipe();
    expect(pipe).toBeTruthy();
  });
});
